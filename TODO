1. in the google sign in process, show a spinner until the login is complete and the profile pic is updated
2. if the user is paid user, then show a golden halo around the profile
3. if the user is already signed in somewhere else, show a modal allowing user to choose whether to terminate other session(s) or stay logged in there


- on desktop browser, limit the max width of the app to something similar to mobile view
- disable screen rotation on mobile, app should always render in portrait mode
- have an icon on the profile to denote admin user


- save the user email and photo_url in the local storage
- when not signed in, show the user from local storage, with an icon to denote not signed in



